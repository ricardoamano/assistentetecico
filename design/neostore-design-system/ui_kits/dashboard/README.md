# Operations dashboard — UI kit

Internal webapp for NEOSTORE operations team. Sidebar nav (operação + comercial), top bar with global search, KPI strip, and events table with status pills. All from `colors_and_type.css` tokens.
